# core/ package — contains the main business logic modules:
#   launcher.py          → Opens apps & URLs
#   workspace_manager.py → Loads, validates, and resolves workspaces from JSON
#   logger.py            → Logs every launch event to logs.txt
